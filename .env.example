# ── Frontend env (copy เป็น .env แล้วใส่ค่าจริง) ──
# ชี้ไป backend local — ต้องมี /api ต่อท้าย
VITE_API_URL=http://localhost:5000/api

# key อัปโหลดรูป certificate (หน้า Verify expert)
VITE_IMGBB_KEY=xxxxxxxxxxxxxxxx
